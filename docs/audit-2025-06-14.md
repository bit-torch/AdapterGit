# Code Audit Report — 2025-06-14 (Updated)

[中文文档](audit-2025-06-14-zh_CN.md)

> Full scan of 48 source files and 119 tests (87 unit + 10 integration + 22 P1), focusing on error handling, security, architectural consistency, and dependency health.

---

## 📊 Project overview

| Metric | Value |
|------|-----|
| Rust source lines | 8,640 |
| Source files | 48 `.rs` |
| Total tests | 119 (all passing ✅) |
| Direct dependencies | 10 |
| Clippy warnings | 0 ✅ |
| rustfmt diff | 0 ✅ |
| `unsafe` code | 0 ✅ |
| `cargo doc` | Pass ✅ |

---

## 🔴 Critical (3) — Must fix

| # | File | Line | Description | Status |
|---|------|-----|------|------|
| **C1** | `src/main.rs` | 103-111 | **`#[cfg(feature = "tag")]` guard incomplete** — `commands::tag::*` calls are not wrapped in `#[cfg(feature = "tag")]`, causing `cargo check --no-default-features` to fail to compile (3 errors). Violates CLAUDE.md rule #10. | 🆕 **New** |
| **C2** | `Cargo.toml` | 13 | **`serde_yaml = "0.9"` deprecated** — crates.io marks it as `deprecated`; should migrate to `serde_yml`. The only usage is in `src/output/mod.rs:49`. | 🆕 **New** |
| **C3** | `Cargo.toml` / `CLAUDE.md` | — | **Version inconsistency** — `Cargo.toml` says `0.6.0`, `CLAUDE.md` says `0.6.1`. | 🆕 **New** |

---

## 🟠 High (3) — Error handling defects

| # | File | Line | Description | Status |
|---|------|-----|------|------|
| **H1** | `src/commands/config_cmd.rs` | 47, 73 | TOML parse failure → `unwrap_or_default()` → silent fallback to empty table. When the original config is corrupted during `config set` / `config unset`, all original data will be lost. | ⚠️ Carried over from last period |
| **H2** | `src/commands/diff.rs` | 62, 94, 207, 211, 250 | `read_blob_content` failure → `unwrap_or_default()` silently falls back to an empty `Vec`. The diff output is inconsistent with the actual repository content, without notifying the user. | ⚠️ Carried over from last period |
| **H3** | `src/core/protocol.rs` | 100-101, 112, 485-487 | **DEBUG `eprintln!` leak** — In production, pkt-line and refs data is printed to stderr, potentially exposing internal repository information. | 🆕 **New** |

---

## 🟡 Medium (5) — Boundary conditions / code robustness

| # | File | Line | Description | Status |
|---|------|-----|------|------|
| **M1** | `src/commands/stash.rs` | 130-161 | Stash linked list traversal has no cycle detection — if a cycle forms in the list (malicious or corrupted), it will loop infinitely. | ⚠️ Carried over from last period |
| **M2** | `src/commands/diff.rs` | 346-406 | The unified diff hunk header is fixed at `@@ -1,N +1,N @@` and does not compute the real starting line number. Inaccurate for large file diffs. | ⚠️ Carried over from last period |
| **M3** | `src/core/protocol.rs` | 607-611 | Chunked encoding detection is based on hex digit heuristics — unreliable, may misjudge in edge cases. | ⚠️ Carried over from last period |
| **M4** | `src/core/objects/tree.rs` / `src/core/index.rs` | 113 / 178 | `hex_to_bytes` duplicated implementation — two identical hex decoding functions. | ⚠️ Carried over from last period |
| **M5** | `src/core/refs.rs` / `src/core/remote_utils.rs` | 65 / 120 | `get_current_branch` duplicated implementation — different signatures (`Option<String>` vs `String`), inconsistent behavior. | ⚠️ Carried over from last period |

---

## 🟢 Low — Code quality improvements (4)

| # | File | Description | Status |
|---|------|------|------|
| **L1** | `src/utils/error.rs` | The `AgitError` enum is fully defined but almost unused by the core layer (core still uses `Box<dyn Error>`). | ⚠️ Carried over from last period |
| **L2** | `src/commands/diff.rs` | `collect_tree_recursive` (302 lines) function body is too long, lacking modular splitting. | 🆕 **New** |
| **L3** | Multiple command files | `println!` / `eprintln!` directly hardcoded output — not conducive to unified JSON/YAML mode. About 80+ hardcoded prints. | 🆕 **New** |
| **L4** | `Cargo.toml` | Missing `cargo-audit` integration — cannot automatically detect known vulnerabilities (CVEs) in dependencies. | 🆕 **New** |

---

## ✅ Fixed (since last audit)

| # | Original description | Fix commit |
|---|--------|---------|
| 1 | 🔴 `rm` deletes file before checking index | `dfa0a3e` |
| 2 | 🔴 `push` ignores remote name, always pushes to origin | `d57b565` |
| 3 | 🔴 `commit` silently falls back when HEAD read fails | `391e680` |
| 4 | 🟠 `remote_utils` `unwrap_or_default()` | `391e680` |
| 5 | 🟠 `branch.rs` list silently returns empty list on failure | `391e680` |
| 6 | 🟠 `reset.rs` silently returns empty string on HEAD read failure | `391e680` |
| 7 | 🟡 AI `DANGEROUS_COMMANDS` dead code | `83ba7a2` |
| 8 | 🟡 `ai_commit_marker()` dead code | `83ba7a2` |
| 9 | 🟢 `format_object_data` triple duplication | Merged into `core/objects/mod.rs` |

**Of the 20 issues from the last audit, 9 were fixed, 7 carried over, and 4 are new.**

---

## 🔍 Special audits

### Security

- ✅ Zero `unsafe` code
- ✅ AI mode dangerous command guard works (`push`, `stash drop`, `branch -D`, `reset`)
- ✅ Network transport supports TLS (`native-tls`)
- ⚠️ `DEBUG eprintln!` may leak internal information in production
- ⚠️ No `cargo-audit` integration — dependency vulnerabilities unknown

### Architecture & consistency

- ✅ Config cascading is correct: env > repo > global > defaults
- ✅ Feature gate is used correctly in core modules (`tag`)
- ❌ The feature gate in the top-level `main.rs` is incomplete (see C1)
- ⚠️ Two `get_current_branch` signatures are inconsistent (see M5)
- ⚠️ `hex_to_bytes` duplicated (see M4)

### Testing

- ✅ 87 unit tests all pass
- ✅ 10 integration tests all pass
- ✅ 22 P1 special tests all pass
- ✅ Coverage: objects, refs, index, compression, hash, ignore, config, protocol, diff, stash, tag, log, rm, mv
- ⚠️ Network-related tests depend on external servers and may be unstable in CI

### Dependency health

| Dependency | Version | Status |
|------|------|------|
| clap | 4.6.1 | ✅ |
| serde | 1.0.228 | ✅ |
| serde_json | 1.0.149 | ✅ |
| serde_yaml | 0.9.34 | ❌ **Deprecated** → migrate to serde_yml |
| flate2 | 1.1.9 | ✅ |
| sha1 | 0.10.6 | ✅ |
| anyhow | 1.0.102 | ✅ |
| url | 2.5.8 | ✅ |
| native-tls | 0.2.18 | ✅ |
| toml | 0.8.23 | ✅ |

---

## 📋 Suggested fix priority order

1. **C1** — Fix the `#[cfg(feature = "tag")]` guard in main.rs (3-line change)
2. **C3** — Unify the version number
3. **H3** — Remove or conditionally compile `DEBUG eprintln!`
4. **C2** — Migrate `serde_yaml` → `serde_yml`
5. **H1** — `config_cmd.rs` `unwrap_or_default()` → proper error
6. **H2** — `diff.rs` `unwrap_or_default()` → proper error
7. **M4/M5** — Eliminate duplicated code
8. **M1** — Stash linked list cycle detection
